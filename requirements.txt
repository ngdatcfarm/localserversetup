# Web Server
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
jinja2==3.1.3

# Camera
# For GPU acceleration (NVDEC/NVENC), install opencv-contrib-python with CUDA support:
#   pip install opencv-contrib-python  (pre-built, may include CUDA)
#   Or build from source with -D WITH_CUDA=ON for full GPU support
opencv-python==4.9.0.80

# Storage
pyyaml==6.0.1

# HTTP Client (PTZ control)
httpx==0.27.0

# Utilities
pydantic==2.5.3

# MQTT Client (local broker communication)
paho-mqtt==1.6.1

# Database (async PostgreSQL/TimescaleDB)
asyncpg==0.29.0

# Automation scheduler (cron expression parsing)
croniter==2.0.1

# File upload support
python-multipart==0.0.6
