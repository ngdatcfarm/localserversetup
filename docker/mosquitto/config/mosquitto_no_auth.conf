# =================================================================
# Mosquitto MQTT Broker - CFarm Local Server
# LAN-only, no authentication required
# =================================================================

# --- Listener ---
listener 1883 0.0.0.0
protocol mqtt

# Allow anonymous connections (no auth required)
allow_anonymous true

# WebSocket port
listener 9001
protocol websockets
http_dir C:/Program Files/Mosquitto/dashboard

# --- Persistence ---
persistence true
persistence_location C:/Program Files/Mosquitto/data/

# --- Logging ---
log_dest file C:/Program Files/Mosquitto/log/mosquitto.log
log_dest stdout
log_type all
connection_messages true
log_timestamp true
log_timestamp_format %Y-%m-%dT%H:%M:%S
